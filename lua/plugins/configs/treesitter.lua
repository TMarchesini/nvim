local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "rust", "python", "markdown", "markdown_inline", "html", "latex", "yaml", "toml" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
